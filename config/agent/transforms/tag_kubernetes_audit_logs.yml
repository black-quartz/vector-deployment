transforms:
  tag_kubernetes_audit_logs:
    type: remap
    inputs:
      - kubernetes_audit_logs
    source: |
      .@source = "kubernetes_audit"
      .@dataset = "kubernetes.audit"
      .@namespace = "default"
